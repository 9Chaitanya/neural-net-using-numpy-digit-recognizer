# neural-net-using-numpy-digit-recognizer - Neural Network from Scratch 🧠

This project implements a basic **fully-connected neural network** using only **NumPy** and **Pandas**, trained on the **MNIST dataset** of handwritten digits.

## 📘 Overview
- Implements forward and backward propagation manually
- Uses ReLU / Sigmoid activations
- Trains using gradient descent
- Achieves ~70% accuracy on train data with 2000 iterations

## 🧩 Requirements
```bash
pip install numpy pandas
